# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/

# Firebase CLI deploy cache (auto-generated, do not commit)
.firebase/

# Build / output artifacts
out/

# Personal planning artifacts (kept in ~/.claude/plans/, not in repo)
md_to_pdf.py
Sakinah_*.pdf

# Python / backend
backend/.env
backend/.venv/
backend/venv/
backend/env/
backend/__pycache__/
backend/**/__pycache__/
backend/.pytest_cache/
backend/*.pyc

# Local-only backend files (not checked in)
backend/validate_jsonl.py
backend/data/stories/moon split seerah_dataset ).jsonl

# Flutter / generated
app/.dart_tool/
app/.flutter-plugins-dependencies
app/.pub/
app/build/
app/coverage/

# Debug screenshots from physical-device testing
app/flutter_*.png

# Temporary legacy folder leftovers after rename
sakinah/.dart_tool/
sakinah/.flutter-plugins-dependencies
sakinah/build/

# Secrets / local configs
app/android/app/google-services.json
backend/serviceAccountKey.json
backend/*serviceAccount*.json
*serviceAccount*.json

# Admin panel — lives in its own git repo
sakinah-admin/
