fastapi
uvicorn
python-dotenv
pydantic
pymongo
python-multipart
sentence-transformers
certifi
httpx
openpyxl
pytest
pytest-asyncio

# ── Multi-modal emotion detection ────────────────────
# Face emotion: HSEmotion (AffectNet-trained, ONNX runtime, ~30MB)
hsemotion-onnx
onnxruntime
# Text emotion: HuggingFace transformers + torch
transformers
torch
# Image handling
Pillow

# ── Auth ───────────────────────────────────────────────
# Verifies Firebase ID tokens on protected admin endpoints. The mobile/
# admin-panel client sends `Authorization: Bearer <id_token>`; we
# validate signature + admin custom claim before letting the request
# through.
firebase-admin
